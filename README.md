```
Usage: miner [OPTIONS]

Settings:
  --pool=Pool                   URL of mining server
  --userworker=User.Worker      miner settings (usually used in viabtc)
  --password=Password           user worker password

./kudarig --pool=bitcoin.viabtc.io:3333 --userworker=yourusername.workername --password=minerpassword

example:
./kudarig --pool=bitcoin.viabtc.io:3333 --userworker=JGXP.PC --password=123

Note:
use the kudarig.py if you are in android
```
